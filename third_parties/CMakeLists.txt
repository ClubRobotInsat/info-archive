# FIXME : cette ligne ne fait pas la même que le `include_directories` du CMakeLists.txt à la racine -- Terae
#include_directories(asio/asio/include include)
add_subdirectory(GLFW)
add_subdirectory(catch)